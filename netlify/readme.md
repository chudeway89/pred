# Netlify deployment notes

This folder is ready to deploy as-is.

**Quick deploy:** Zip the contents of *this* folder (so `index.html` is at the root of the zip), then drag the zip onto <https://app.netlify.com/drop>.

The site is a single self-contained HTML file. No build step required.
